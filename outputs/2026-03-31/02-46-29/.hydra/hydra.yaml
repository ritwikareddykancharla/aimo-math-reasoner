hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - actor_rollout_ref.model.path=/data/models/gpt-oss-120b
    - actor_rollout_ref.model.trust_remote_code=true
    - actor_rollout_ref.model.use_remove_padding=true
    - actor_rollout_ref.model.enable_gradient_checkpointing=true
    - actor_rollout_ref.rollout.name=sglang
    - actor_rollout_ref.rollout.mode=async
    - actor_rollout_ref.rollout.tensor_model_parallel_size=8
    - actor_rollout_ref.rollout.gpu_memory_utilization=0.7
    - actor_rollout_ref.rollout.n=8
    - actor_rollout_ref.rollout.temperature=1.0
    - actor_rollout_ref.rollout.top_p=1.0
    - +actor_rollout_ref.rollout.min_p=0.02
    - actor_rollout_ref.rollout.max_model_len=65536
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1
    - ++actor_rollout_ref.rollout.multi_turn.enable=true
    - ++actor_rollout_ref.rollout.multi_turn.max_user_turns=8
    - ++actor_rollout_ref.rollout.multi_turn.max_assistant_turns=8
    - ++actor_rollout_ref.rollout.multi_turn.format=gpt-oss
    - +actor_rollout_ref.rollout.agent.tool_parser=gpt-oss
    - +actor_rollout_ref.rollout.agent.agent_loop_config_path=/home/ssm-user/aimo-math-reasoner/training/grpo/agent.yaml
    - actor_rollout_ref.rollout.val_kwargs.top_p=1.0
    - actor_rollout_ref.rollout.val_kwargs.temperature=1.0
    - actor_rollout_ref.rollout.val_kwargs.n=1
    - actor_rollout_ref.actor.use_kl_loss=true
    - actor_rollout_ref.actor.kl_loss_coef=0.001
    - actor_rollout_ref.actor.kl_loss_type=low_var_kl
    - actor_rollout_ref.actor.clip_ratio_low=0.2
    - actor_rollout_ref.actor.clip_ratio_high=0.28
    - actor_rollout_ref.actor.clip_ratio_c=10.0
    - actor_rollout_ref.actor.optim.lr=5e-7
    - actor_rollout_ref.actor.use_dynamic_bsz=true
    - actor_rollout_ref.actor.ppo_mini_batch_size=16
    - actor_rollout_ref.actor.ppo_max_token_len_per_gpu=69632
    - actor_rollout_ref.actor.ulysses_sequence_parallel_size=4
    - actor_rollout_ref.actor.fsdp_config.param_offload=false
    - actor_rollout_ref.actor.fsdp_config.optimizer_offload=false
    - actor_rollout_ref.actor.fsdp_config.dtype=bfloat16
    - actor_rollout_ref.actor.fsdp_config.model_dtype=bfloat16
    - actor_rollout_ref.actor.checkpoint.save_contents=['model']
    - actor_rollout_ref.ref.log_prob_max_token_len_per_gpu=139264
    - actor_rollout_ref.ref.fsdp_config.param_offload=false
    - actor_rollout_ref.ref.fsdp_config.dtype=bfloat16
    - actor_rollout_ref.ref.fsdp_config.model_dtype=bfloat16
    - algorithm.adv_estimator=grpo
    - algorithm.use_kl_in_reward=false
    - data.train_files=/home/ssm-user/aimo-math-reasoner/data/grpo_aops_acc0125.parquet
    - data.val_files=/home/ssm-user/aimo-math-reasoner/data/grpo_aops_acc0125.parquet
    - data.return_raw_chat=true
    - data.train_batch_size=128
    - data.max_prompt_length=2048
    - data.max_response_length=32768
    - data.filter_overlong_prompts=true
    - data.truncation=error
    - reward.reward_model.enable=false
    - reward.custom_reward_function.path=/home/ssm-user/aimo-math-reasoner/training/grpo/reward_fn.py
    - reward.custom_reward_function.name=compute_score
    - trainer.nnodes=2
    - trainer.n_gpus_per_node=8
    - trainer.total_epochs=1
    - trainer.project_name=aimo3-grpo
    - trainer.experiment_name=gpt-oss-120b-round1
    - trainer.save_freq=50
    - trainer.val_before_train=false
    - trainer.test_freq=-1
    - trainer.resume_mode=auto
    - trainer.default_local_dir=/data/checkpoints/grpo-round1
    - trainer.max_actor_ckpt_to_keep=3
    - trainer.log_val_generations=50
    - +trainer.nccl_timeout=1800
  job:
    name: main_ppo
    chdir: null
    override_dirname: ++actor_rollout_ref.rollout.multi_turn.enable=true,++actor_rollout_ref.rollout.multi_turn.format=gpt-oss,++actor_rollout_ref.rollout.multi_turn.max_assistant_turns=8,++actor_rollout_ref.rollout.multi_turn.max_user_turns=8,+actor_rollout_ref.rollout.agent.agent_loop_config_path=/home/ssm-user/aimo-math-reasoner/training/grpo/agent.yaml,+actor_rollout_ref.rollout.agent.tool_parser=gpt-oss,+actor_rollout_ref.rollout.min_p=0.02,+trainer.nccl_timeout=1800,actor_rollout_ref.actor.checkpoint.save_contents=['model'],actor_rollout_ref.actor.clip_ratio_c=10.0,actor_rollout_ref.actor.clip_ratio_high=0.28,actor_rollout_ref.actor.clip_ratio_low=0.2,actor_rollout_ref.actor.fsdp_config.dtype=bfloat16,actor_rollout_ref.actor.fsdp_config.model_dtype=bfloat16,actor_rollout_ref.actor.fsdp_config.optimizer_offload=false,actor_rollout_ref.actor.fsdp_config.param_offload=false,actor_rollout_ref.actor.kl_loss_coef=0.001,actor_rollout_ref.actor.kl_loss_type=low_var_kl,actor_rollout_ref.actor.optim.lr=5e-7,actor_rollout_ref.actor.ppo_max_token_len_per_gpu=69632,actor_rollout_ref.actor.ppo_mini_batch_size=16,actor_rollout_ref.actor.ulysses_sequence_parallel_size=4,actor_rollout_ref.actor.use_dynamic_bsz=true,actor_rollout_ref.actor.use_kl_loss=true,actor_rollout_ref.model.enable_gradient_checkpointing=true,actor_rollout_ref.model.path=/data/models/gpt-oss-120b,actor_rollout_ref.model.trust_remote_code=true,actor_rollout_ref.model.use_remove_padding=true,actor_rollout_ref.ref.fsdp_config.dtype=bfloat16,actor_rollout_ref.ref.fsdp_config.model_dtype=bfloat16,actor_rollout_ref.ref.fsdp_config.param_offload=false,actor_rollout_ref.ref.log_prob_max_token_len_per_gpu=139264,actor_rollout_ref.rollout.gpu_memory_utilization=0.7,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.max_model_len=65536,actor_rollout_ref.rollout.mode=async,actor_rollout_ref.rollout.n=8,actor_rollout_ref.rollout.name=sglang,actor_rollout_ref.rollout.temperature=1.0,actor_rollout_ref.rollout.tensor_model_parallel_size=8,actor_rollout_ref.rollout.top_p=1.0,actor_rollout_ref.rollout.val_kwargs.n=1,actor_rollout_ref.rollout.val_kwargs.temperature=1.0,actor_rollout_ref.rollout.val_kwargs.top_p=1.0,algorithm.adv_estimator=grpo,algorithm.use_kl_in_reward=false,data.filter_overlong_prompts=true,data.max_prompt_length=2048,data.max_response_length=32768,data.return_raw_chat=true,data.train_batch_size=128,data.train_files=/home/ssm-user/aimo-math-reasoner/data/grpo_aops_acc0125.parquet,data.truncation=error,data.val_files=/home/ssm-user/aimo-math-reasoner/data/grpo_aops_acc0125.parquet,reward.custom_reward_function.name=compute_score,reward.custom_reward_function.path=/home/ssm-user/aimo-math-reasoner/training/grpo/reward_fn.py,reward.reward_model.enable=false,trainer.default_local_dir=/data/checkpoints/grpo-round1,trainer.experiment_name=gpt-oss-120b-round1,trainer.log_val_generations=50,trainer.max_actor_ckpt_to_keep=3,trainer.n_gpus_per_node=8,trainer.nnodes=2,trainer.project_name=aimo3-grpo,trainer.resume_mode=auto,trainer.save_freq=50,trainer.test_freq=-1,trainer.total_epochs=1,trainer.val_before_train=false
    id: ???
    num: ???
    config_name: ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /home/ssm-user/aimo-math-reasoner
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /home/ssm-user/.local/lib/python3.12/site-packages/verl/trainer/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /home/ssm-user/aimo-math-reasoner/outputs/2026-03-31/02-46-29
    choices:
      algorithm@algorithm.rollout_correction: rollout_correction
      reward: reward
      model@actor_rollout_ref.model: hf_model
      rollout@actor_rollout_ref.rollout: rollout
      data: legacy_data
      model_engine: dp
      critic: dp_critic
      critic/../engine@critic.model.fsdp_config: fsdp
      critic/../optim@critic.optim: fsdp
      ref@actor_rollout_ref.ref: dp_ref
      ref/../engine@actor_rollout_ref.ref.fsdp_config: fsdp
      actor@actor_rollout_ref.actor: dp_actor
      actor/../engine@actor_rollout_ref.actor.fsdp_config: fsdp
      actor/../optim@actor_rollout_ref.actor.optim: fsdp
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
